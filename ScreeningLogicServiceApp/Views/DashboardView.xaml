<UserControl x:Class="ScreeningLogicServiceApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#E9F6FB" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- How many names question -->
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="40,30,40,10">
            <TextBlock Text="How many  names do you want to process?" FontSize="28" Foreground="#1B5B6A" VerticalAlignment="Center"/>
            <ComboBox x:Name="NamesToProcessComboBox" Width="160" Height="38" Margin="20,0,0,0" VerticalAlignment="Center">
                <ComboBoxItem Content="1"/>
                <ComboBoxItem Content="2"/>
                <ComboBoxItem Content="5" IsSelected="True"/>
                <ComboBoxItem Content="10"/>
                <ComboBoxItem Content="25"/>
                <ComboBoxItem Content="50"/>
            </ComboBox>
        </StackPanel>

        <!-- Current Application Status label -->
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Text="Current Application Status:" FontSize="24" Foreground="#1B5B6A" Margin="40,30,0,15"/>

        <!-- Status cards -->
        <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Margin="30,10,30,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" BorderBrush="#9DB9C2" BorderThickness="1" CornerRadius="4" Background="#F6FCFF" Padding="20" Margin="15">
                <TextBlock Text="Stopped" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22" Foreground="#1B5B6A"/>
            </Border>
            <Border Grid.Column="1" BorderBrush="#9DB9C2" BorderThickness="1" CornerRadius="4" Background="#F6FCFF" Padding="20" Margin="15">
                <TextBlock Text="Processing Names" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22" Foreground="#1B5B6A"/>
            </Border>
            <Border Grid.Column="2" BorderBrush="#9DB9C2" BorderThickness="1" CornerRadius="4" Background="#F6FCFF" Padding="20" Margin="15">
                <StackPanel>
                    <TextBlock Text="Stand By" HorizontalAlignment="Center" FontSize="22" Foreground="#1B5B6A"/>
                    <TextBlock Text="(waiting to process next batch)" HorizontalAlignment="Center" FontSize="16" Foreground="#1B5B6A"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Action buttons -->
        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,20" >
            <Button x:Name="StartButton" Content="Start" Width="260" Height="64" Style="{StaticResource StartButtonStyle}" FontSize="22" Margin="20,0" Click="StartButton_Click"/>
            <Button x:Name="StopButton" Content="Stop" Width="260" Height="64" Style="{StaticResource StopButtonStyle}" FontSize="22" Margin="20,0" Click="StopButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
